# Anthropic API key used ONLY by the serverless function in /api/agent.js.
# Never expose this in frontend code.
ANTHROPIC_API_KEY=

# Optional: override the model used by the agent endpoint.
# ANTHROPIC_MODEL=claude-sonnet-5
