{
  "name": "jerry-companion",
  "version": "0.2.0",
  "private": true,
  "description": "Jerry - an AI mood-reactive desk companion built on an Arduino Uno",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env scripts/dev-server.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:agent": "node --env-file-if-exists=.env scripts/check-agent.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
